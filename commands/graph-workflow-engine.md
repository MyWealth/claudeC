# 加载并执行 Graph Workflow Engine

读取并执行以下技能文件中的所有指令：
`./.claude/skills/graph-workflow-engine/SKILL.yaml`

该文件包含：
- 技能名称、版本、描述
- 触发方式（patterns、file_extensions）
- 完整的 system_prompt（核心能力、工作流程、响应格式）

请按照 SKILL.yaml 中的 system_prompt 部分来响应用户。
